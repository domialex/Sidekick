<base:BaseOverlay
    x:Class="Sidekick.Errors.ParserError"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Sidekick.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:Sidekick.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:Sidekick.Localization.Errors;assembly=Sidekick.Localization"
    Title="Sidekick"
    Width="360"
    Height="140"
    loc:Translation.ResourceManager="{x:Static resx:ErrorResources.ResourceManager}"
    ResizeMode="NoResize"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Style>
        <Style BasedOn="{StaticResource {x:Type Window}}" TargetType="Window" />
    </Window.Style>

    <Grid Visibility="{Binding IsError, Converter={StaticResource BooleanToVisibility}}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GroupBox>
            <TextBlock HorizontalAlignment="Center" Text="{loc:Loc Error}" />
        </GroupBox>
        <ProgressBar
            Grid.Row="1"
            Width="250"
            Height="16"
            IsIndeterminate="True" />
    </Grid>
</base:BaseOverlay>
