<Window x:Class="Sidekick.Windows.AdvancedSearch.AdvancedSearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sidekick.Windows.AdvancedSearch"
        mc:Ignorable="d"
        Title="AdvancedSearchView" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Main.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Style>
        <Style BasedOn="{StaticResource WindowOverlay}" TargetType="Window"/>
    </Window.Style>
    
    <Grid x:Name="gridAdvancedSearch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="columnType"/>
            <ColumnDefinition x:Name="columnAttribute"/>
            <ColumnDefinition x:Name="columnminValue"/>
            <ColumnDefinition x:Name="columnMaxValue"/>
            <ColumnDefinition x:Name="columnEnabled"/>
        </Grid.ColumnDefinitions>

    </Grid>
</Window>
